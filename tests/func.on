extern fn printf(message: String);

fn person(name: String, age: usize) { 
    printf(message: "Name: ");
    printf(message: name);
    printf(message: "\n");
}

fn program() {
    let name = "John";
    let age = 40;
    person(name: name, age: age);
}
